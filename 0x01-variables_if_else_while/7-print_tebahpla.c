#include <stdio.h>

/**
 * main - prints lowercase
 * letters in reverse order with putchar
 * Return: 0 always (success)
 */
int main(void)
{
	int ch;

	for (n = 'z'; n >= 'a'; n--)
	{
		putchar(n);
	}
	putchar('\n');
	return (0);
}
