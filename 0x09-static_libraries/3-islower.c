#include "main.h"

/**
 * _islower - checks for lowercase
 *  characters
 *  @c: character to check
 *  Return: 0 or uppercase and 1 for lowercase
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
