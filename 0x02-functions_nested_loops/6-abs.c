#include "main.h"

/**
 * _abs - computes absolute
 * absolute value of an integer
 * @n: the integert to check
 * Return: 0
 */
int _abs(int n)
{
	if (n < 0)
	{
		return (-n);
	}
	else
	{
		return (n);
	}
}
